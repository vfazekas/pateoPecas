<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pateo peças Login</title>
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <!-- Custom CSS -->
  <style>
    body {
      background-color: #f9f9f9;
    }
    .login-container {
      max-width: 400px;
      margin: 0 auto;
      padding: 30px 40px;
      border-radius: 10px;
      background-color: #fff;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
    .login-logo {
      display: block;
      margin: 0 auto 30px;
      max-width: 200px;
    }
    .login-form input[type="text"],
    .login-form input[type="password"] {
      padding: 10px;
      border-radius: 5px;
      border: 1px solid #ccc;
    }
    .login-form button {
      padding: 10px;
      border-radius: 5px;
      background-color: #ffa500;
      border: none;
      color: #fff;
      cursor: pointer;
    }
    .login-form button:hover {
      background-color: #ff7f00;
    }
    .login-footer {
      text-align: center;
      margin-top: 20px;
    }
    .login-footer a {
      color: #888;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <div class="login-container">
     <img class="login-logo" src="piston.gif" alt="piston" width="100" height="100">
      <form class="login-form" action="" method="POST">
        <div class="form-group">
          <label for="username">Usuário</label>
          <input type="text" class="form-control" id="username" placeholder="Digite seu usuário" name="username" required>
        </div>
        <div class="form-group">
          <label for="password">Senha</label>
          <input type="password" class="form-control" id="password" placeholder="Digite sua senha" name="password" required>
        </div>
        <button type="submit" class="btn btn-primary btn-block" id="btn_login">Entrar</button>
      </form>    
    </div>
  </div>
</body>
</html>